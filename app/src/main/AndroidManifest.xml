<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.genvict.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TextViewActivity" />
        <activity android:name=".activity.QQStepViewActivity" />
        <activity android:name=".activity.ColorTrackActivity" />
        <activity android:name=".activity.ProgressViewActivity" />
        <activity android:name=".activity.RatingBarActivity" />
        <activity android:name=".activity.LoadViewActivity" />
        <activity android:name=".activity.LetterSideBarActivity" />
        <activity android:name=".activity.TagLayoutActivity" />
        <activity android:name=".activity.TouchViewActivity" />
        <activity android:name=".activity.SidingMenuActivity" />
        <activity android:name=".activity.LockPatternActivity"></activity>
    </application>

</manifest>